Clicking on "Delete" in the actions row deletes the customer's data immediately without confirmation

The "add" and the "update" brings you to a page in which you need to tick off the "Confirm?" checkbox
